{
  "name": "syncboard",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Realtime collaborative kanban board with live presence and cursors",
  "author": "Aryan Jaigirdar <jaigirdar.aryan@gmail.com>",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "dev": "concurrently -k -n server,web -c blue,magenta \"npm run dev -w server\" \"npm run dev -w web\"",
    "build": "npm run build -w web && npm run build -w server",
    "start": "npm run start -w server",
    "test": "npm run test -w server",
    "typecheck": "npm run typecheck -w server && npm run typecheck -w web"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
